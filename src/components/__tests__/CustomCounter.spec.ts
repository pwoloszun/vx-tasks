import { render } from '@testing-library/vue';
import userEvent from '@testing-library/user-event';

import CustomCunter from '../CustomCounter.vue';

function renderCustomCunter(initialValue = 0) {
  return null; // TODO
}

describe('CustomCunter', () => {
  xit('should render passed props', async () => {
  });

  xit('should emit @increment event when "Increment" button clicked', async () => {
  });

  xit('should emit @decrement event when "Decrement" button clicked', async () => {
  });
});
